# Import laraflask.core.Core class
from laraflask.core import Core

# Create a Core instance
core = Core()

# Run the Flask app
if __name__ == '__main__':
    # Run the Core instance
    core.run()

# Create a virtual environment with the following command:
# $ python3 -m venv venv
    
# Activate the virtual environment with the following command:
# $ source venv/bin/activate
    
# Install laraflask with the following command:
# pip install laraflask 
# or install the requirements.txt file with the following command:
# pip install -r requirements.txt

# Run the Flask app with the following command:
# $ python run.py